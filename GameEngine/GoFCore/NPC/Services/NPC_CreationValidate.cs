using GameEngine.NPC.Interfaces;
using GameEngine.NPC.Interfaces.Services;
using GameEngine.NPC.Interfaces.SpecializationArchetypes;

namespace GameEngine.NPC.Services
{
    public class NPC_CreationValidate /*: INPC_CreationValidate*/
    {
/*        public INPC_Enemy Validate(int playerLevel)
        {
            var npcBuilder = new NPC_Builder();
            switch (playerLevel)
            {
                case >= 1 and < 6:
                    return npcBuilder.Build(IAnimal);
                default:
                    break;
            }
        }*/
    }
}
